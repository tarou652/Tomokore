# トモコレ フェイスペイント髪型変換アプリ 仕様書 v5

## 1. アプリ概要

### アプリ名

Tomodachi Hair Trace

### 目的

キャラクター画像・立ち絵・顔画像から、
トモダチコレクション新作のフェイスペイント用のドット絵を生成する。

### 動作環境

- ブラウザ完結（サーバー不要）
- 強めのPC（ローカル）での使用を前提
- WebGPU 対応ブラウザ推奨（Chrome 最新版など）

---

## 2. フェイスペイント仕様

### キャンバスサイズ

256 × 256 px（最大）

---

## 3. 入力仕様

### 対応形式

- PNG
- JPG
- WEBP

### 推奨画像条件

- 正面顔
- 髪が隠れていない
- 背景が単純
- アニメ立ち絵

---

## 4. 機能仕様

### 4-1. 画像アップロード

- クリックまたはドロップで読み込み

### 4-2. 正方形クロップ

- コーナーをドラッグして範囲調整
- 正方形固定

### 4-3. 解像度選択

変換前にターゲットサイズを選ぶ。AI変換・Pixel Snap 両方に適用される。

| 選択肢 | サイズ     |
| ------ | ---------- |
| 32     | 32 × 32 px |
| 64     | 64 × 64 px |
| 128    | 128 × 128 px |
| 256    | 256 × 256 px |

### 4-4. AI img2img 変換

- Transformers.js（WebGPU）を使用
- 選択した解像度をモデルに渡してドット絵風に変換
- ブラウザ内で完結（画像の外部送信なし）

### 4-5. Pixel Snap 処理

AI変換後の画像をより本物のドット絵に整える。

- 色数削減スライダー（8 〜 32 色）
- 近い色を同じ色にスナップ
- エッジをグリッドに沿って整列

### 4-6. ズーム表示

- 1x / 2x / 4x / 8x / 16x
- グリッド線表示
- 座標ラベル表示
- ピクセルホバーで座標・HEX表示

### 4-7. カラーパレット

- Pixel Snap 後の使用色を一覧表示
- クリックで HEX コピー
- 使用頻度順・使用率バー付き

### 4-8. PNG 出力

- ドット絵をそのままダウンロード

---

## 5. UI 構成

```
左カラム               右カラム
─────────────────────────────────
画像アップロード       ドット絵プレビュー（ズーム）
正方形クロップ
解像度選択（32/64/128/256）
AI変換ボタン           カラーパレット
色数スライダー
Pixel Snapボタン
```

---

## 6. 技術構成

| 役割             | 技術                          |
| ---------------- | ----------------------------- |
| フロントエンド   | Nuxt 4 (SPA)                  |
| AI img2img 変換  | Transformers.js + WebGPU      |
| Pixel Snap       | Canvas API（ブラウザ内処理）  |
| スタイル         | Tailwind CSS v4 / Nuxt UI     |

---

## 7. 実装フェーズ

### Phase 1（完了）
- 画像アップロード
- 正方形クロップ
- ダウンスケール変換
- ズーム表示
- カラーパレット
- PNG 出力

### Phase 2（次）
- 解像度選択を 32 追加
- Pixel Snap 処理（色数削減・スナップ）
- 色数スライダー UI

### Phase 3
- Transformers.js による AI img2img 変換
- 解像度をモデルに渡す処理
- WebGPU フォールバック処理

---

## 8. 設計思想

このアプリは、

❌ 写真を忠実再現するアプリ

ではなく、

✅ トモコレで「似せやすくする」ドット絵を作るアプリ

である。
