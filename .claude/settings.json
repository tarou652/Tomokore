{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(npx nuxi *)",
      "Bash(git log *)",
      "Bash(git status)",
      "Bash(git diff *)"
    ],
    "deny": ["Bash(rm -rf *)", "Bash(git push --force*)", "Write(.env*)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd c:/code/tomokore && npx prettier --write \"${TOOL_INPUT_FILE_PATH}\" 2>/dev/null || true",
            "statusMessage": "Prettier でフォーマット中..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"${TOOL_INPUT_COMMAND}\" | grep -qE '^(rm -rf|git push --force)' && echo '{\"hookSpecificOutput\":{\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"危険なコマンドはブロックされています\"}}' && exit 1 || exit 0"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd c:/code/tomokore && STATUS=$(git status --short 2>/dev/null); if [ -n \"$STATUS\" ]; then echo ''; echo '╔══════════════════════════════════════╗'; echo '║  📝 未コミットの変更があります        ║'; echo '╚══════════════════════════════════════╝'; echo ''; git status --short; echo ''; echo 'コミットしますか？ → git add / git commit'; echo ''; fi",
            "statusMessage": "変更チェック中..."
          }
        ]
      }
    ]
  }
}
